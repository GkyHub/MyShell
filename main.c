#include <stdio.h>

int main()
{

    return 0;
}

int RecvArg(int fd, char *buffer, int *pos)
{
    char c;
    int c_pos = 0;
    int t_num = 0;

    while(read(fd, &c, 1) > 0) {
        if ()
    }
}
